
```
python3 -m http.server 8080 --bind 0.0.0.0
wget http://192.168.0.111:8080/venv.zip -O /tmp/apigw_1_4.tar
```

```
- **Win + R →** `wf.msc`
    
- **Правила для входящих подключений**
    
- **Создать правило…**
    
- Тип → **Порт**
    
- TCP → **8080**
    
- **Разрешить подключение**
    
- Профили → **Частный** (и **Домен**, если есть)
    
- Имя → `Python HTTP Server 8080`
```