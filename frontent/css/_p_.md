
```
p::first-line {  
}
```
`::first-line` всегда выбирает первую строку внутри элемента

- `article > p` — только **непосредственные дочерние** элементы `<p>`.
- `article p` — все `<p>` внутри `<article>`, включая вложенные.